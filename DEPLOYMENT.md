# Размещение бота на Bothost

Этот документ содержит инструкции по размещению Telegram-бота на платформе [Bothost](https://bothost.ru/) - специализированном хостинге для ботов мессенджеров.

## Преимущества Bothost

- 🇷🇺 Российская платформа
- 💰 Бесплатный тариф для начала
- 🚀 Автоматический деплой из GitHub/GitLab
- 📊 Мониторинг и логи в реальном времени
- 🔒 SSL сертификаты из коробки
- 🐳 Поддержка Docker контейнеров
- 📝 Удобная панель управления

## Тарифы

**Бесплатный тариф (Starter):**
- 1 бот
- 1 vCPU
- 256 MB RAM
- 0.5 GB SSD
- Авто-деплой
- Логи в реальном времени
- Поддержка сообщества

Для нашего бота этого достаточно! 🎉

## Развертывание бота

### Шаг 1: Регистрация на Bothost

1. Перейдите на [bothost.ru](https://bothost.ru/)
2. Нажмите "Регистрация"
3. Создайте аккаунт (можно использовать GitHub/GitLab для быстрой регистрации)

### Шаг 2: Подготовка репозитория

Убедитесь, что ваш код находится в Git репозитории (GitHub или GitLab):

```bash
# Если еще не в Git репозитории
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш_username/ваш_репозиторий.git
git push -u origin main
```

**Важно:** Не загружайте файл `config.py` с токенами в Git! Используйте только `config.py.example`.

### Шаг 3: Создание проекта на Bothost

1. Войдите в панель управления Bothost
2. Нажмите "Создать проект" или "Новый бот"
3. Выберите тип бота: **Telegram бот**
4. Выберите способ развертывания: **Git репозиторий**
5. Подключите ваш GitHub/GitLab аккаунт (если еще не подключен)
6. Выберите репозиторий с вашим ботом
7. Выберите ветку (обычно `main` или `master`)

### Шаг 4: Настройка переменных окружения

В настройках проекта добавьте переменные окружения:

1. Перейдите в раздел "Переменные окружения" или "Environment Variables"
2. Добавьте следующие переменные:

   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   YANDEX_DISK_TOKEN=ваш_токен_яндекс_диска
   YANDEX_DISK_FOLDER=/wisdom_card
   ```

3. Сохраните переменные

**Важно:** На бесплатном тарифе переменные окружения могут быть недоступны. В этом случае создайте файл `.env` в корне проекта (но не добавляйте его в Git!) или используйте другой способ.

### Шаг 5: Настройка запуска

Bothost автоматически определит Python проект, но убедитесь, что:

1. Файл `requirements.txt` находится в корне проекта
2. Точка входа - файл `bot.py`
3. Команда запуска: `python bot.py`

Если нужно указать команду запуска вручную, используйте:
```
python bot.py
```

Или для Python 3:
```
python3 bot.py
```

### Шаг 6: Деплой

1. Нажмите кнопку "Развернуть" или "Deploy"
2. Bothost автоматически:
   - Установит зависимости из `requirements.txt`
   - Запустит бота
   - Покажет логи в реальном времени

3. Проверьте логи на наличие ошибок
4. Бот запущен и работает! 🎉

### Шаг 7: Автоматический деплой

После первого развертывания при каждом пуше в выбранную ветку (обычно `main`) бот будет автоматически обновляться:

1. Загрузите изменения в Git:
   ```bash
   git add .
   git commit -m "Обновление бота"
   git push
   ```

2. Bothost автоматически обнаружит изменения и перезапустит бота

## Управление ботом

### Просмотр логов

В панели управления Bothost:
1. Откройте ваш проект
2. Перейдите в раздел "Логи" или "Logs"
3. Вы увидите логи в реальном времени

### Перезапуск бота

1. В панели управления откройте проект
2. Нажмите кнопку "Перезапустить" или "Restart"
3. Бот перезапустится с текущей версией кода

### Мониторинг ресурсов

В панели управления можно отслеживать:
- Использование CPU
- Использование RAM
- Использование диска

## Решение проблем

### Бот не запускается

1. **Проверьте логи** в панели управления
2. **Проверьте токены** - убедитесь, что переменные окружения установлены правильно
3. **Проверьте requirements.txt** - все зависимости должны быть указаны

### Ошибка "ModuleNotFoundError"

Убедитесь, что все зависимости указаны в `requirements.txt`:
```
python-telegram-bot==20.7
python-dotenv==1.0.0
requests==2.31.0
```

### Бот не отвечает

1. Проверьте токен Telegram бота в переменных окружения
2. Проверьте логи на наличие ошибок
3. Убедитесь, что бот запущен (статус в панели управления)

### Недостаточно памяти (256 MB)

Если бот использует слишком много памяти:
- Оптимизируйте код
- Рассмотрите переход на тариф Basic (99₽/мес, 1 GB RAM)

## Миграция с локального компьютера

Если бот уже работает локально:

1. Убедитесь, что `config.py` не загружен в Git (добавлен в `.gitignore`)
2. Создайте `config.py.example` с примером без токенов
3. Загрузите код в Git репозиторий
4. Создайте проект на Bothost
5. Добавьте переменные окружения с токенами
6. Разверните бота

## Безопасность

1. **Никогда не публикуйте токены в Git!**
   - Файл `config.py` должен быть в `.gitignore`
   - Используйте `config.py.example` для примера

2. **Используйте переменные окружения** на хостинге

3. **Регулярно обновляйте зависимости**:
   ```bash
   pip list --outdated
   pip install --upgrade package_name
   ```

## Обновление бота

1. Внесите изменения в код локально
2. Протестируйте локально (опционально)
3. Загрузите изменения в Git:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
4. Bothost автоматически обнаружит изменения и перезапустит бота
5. Проверьте логи в панели управления

## Полезные ссылки

- [Официальный сайт Bothost](https://bothost.ru/)
- [Документация Bothost](https://bothost.ru/docs) (если доступна)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Python Telegram Bot Library](https://python-telegram-bot.org/)

## Переход на платный тариф

Если вам нужны дополнительные ресурсы:

**Basic (99₽/мес):**
- 5 ботов
- 2 vCPU
- 1 GB RAM
- 5 GB SSD
- Переменные окружения
- Авто-бэкапы
- Email алерты

**Pro (250₽/мес):**
- 20 ботов
- 4 vCPU
- 2 GB RAM
- 15 GB SSD
- Авто-скейлинг
- Выделенные ресурсы
- Support 24/7

Переход на платный тариф происходит в один клик в панели управления.
